doctrine:
    dbal:
        # Use file-backed SQLite for tests to ensure schema
        # created by console commands is visible to the PHPUnit process.
        url: 'sqlite:///%kernel.project_dir%/var/test.sqlite'
        driver: 'pdo_sqlite'
