# =============================================================================
# Vehicle Management System - Frontend Demo Environment
# =============================================================================
# These REACT_APP_* values are baked into the JS bundle at build time.
# They are passed as build args in docker-compose.demo.yml, but this
# file can be used for local development in demo mode.
# =============================================================================

###> API Configuration ###
# /api works behind any reverse proxy — no hardcoded domain needed
REACT_APP_API_URL=/api
###< API Configuration ###

###> Demo Mode ###
# Displays demo credentials on the login page
REACT_APP_DEMO_MODE=true
###< Demo Mode ###

###> Password Policy ###
# >8 chars, requires uppercase, lowercase, and numbers
REACT_APP_PASSWORD_POLICY=^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$
###< Password Policy ###
