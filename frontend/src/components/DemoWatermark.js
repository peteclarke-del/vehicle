/**
 * DemoWatermark placeholder.
 *
 * The actual watermark is now injected by an inline <script> in
 * public/index.html so it works regardless of React/webpack/CRA
 * build pipeline.  This component is kept as a no-op so existing
 * imports (App.js) don't break.
 */
const DemoWatermark = () => null;

export default DemoWatermark;
