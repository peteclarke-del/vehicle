import React from 'react';

// Legacy stub: `InsuranceDialog` replaced by `PolicyDialog`.
// Keep a minimal stub to avoid module-not-found errors until callers are migrated.
const InsuranceDialog = () => null;

export default InsuranceDialog;
